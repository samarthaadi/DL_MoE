# 05_results

- `main_results.csv` — all 15 experiments × 2 tasks, mean ± std across seeds 42 and 123
- `ablations.csv` — four ablation groups: temperature τ, router input, router architecture, routing granularity
- `figures/` → see `results/figures/` in repo root (interpretability PNGs + stats JSON)
- `logs/` → see `results/logs/` in repo root (per-epoch CSV training logs for all 60 runs)
